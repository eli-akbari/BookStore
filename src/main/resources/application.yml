spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:book_store;MODE=Oracle
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    root: INFO

server:
  port: 8080
